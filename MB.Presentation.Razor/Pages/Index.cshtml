@page
@model IndexModel
@{
    ViewData["Title"] = "Index";
}

<main class="posts-listing col-lg-12">
    <div class="container">
        <div class="row">

            @foreach (var article in Model.Articles)
            {
                <div class="post col-xl-6">
                    <div class="post-thumbnail">
                        <a asp-page="./ArticleDetail" asp-route-id="@article.Id">
                            <img src="@article.PictureUrl"
                                 alt="@article.Title"
                                 title="@article.Title"
                                 class="img-thumbnail"
                                 style="width: 35em;">
                        </a>
                    </div>
                    <div class="post-details">
                        <div class="post-meta d-flex justify-content-between">
                            <div class="date meta-last">@article.CreationTime</div>
                            <div class="category">
                                <a asp-page="./ArticleDetail" asp-route-id="@article.Id">@article.ArticleCategory</a>
                            </div>
                        </div>
                        <a asp-page="./ArticleDetail" asp-route-id="@article.Id">
                            <h3 class="h4">@article.Title</h3>
                        </a>
                        <p class="text-monospace">
                            @article.ShortDescription
                        </p>
                        <footer class="post-footer d-flex align-items-center">
                            <div class="comments meta-last"><i class="icon-comment"></i></div>
                        </footer>
                    </div>
                </div>
             }

        </div>
    </div>
</main>